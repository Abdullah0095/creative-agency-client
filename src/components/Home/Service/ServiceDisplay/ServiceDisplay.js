import React from 'react';

const ServiceDisplay = ({item}) => {
    
    return (
        <div>
            {/* <h1>{item.description}</h1> */}
        </div>
    );
};

export default ServiceDisplay;